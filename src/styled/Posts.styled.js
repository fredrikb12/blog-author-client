import styled from "styled-components";

export const StyledPosts = styled.main`
  max-width: clamp(260px, 80vw, 900px);
  display: flex;
  flex-direction: column;
  margin: auto;
  border: 1px solid black;
`;

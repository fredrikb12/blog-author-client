import styled from "styled-components";

export const StyledButton = styled.button`
  font-size: 1.2rem;
  padding: 6px 16px;
  border-radius: 12px;
  border: none;

  &:hover {
    cursor: pointer;
  }
`;
